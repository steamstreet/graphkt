# A very simple schema to test server processing.
schema {
    query: Query
}

scalar PhoneNumber

type Query {
    person(id: String!): Person
}

type Person {
    name: String
    friends: [String!]!

    phoneNumber: PhoneNumber
}